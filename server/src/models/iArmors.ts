export interface IArmor {
    id: number;
    name: string;
    decoSlots: number;
    skills: number[];
    skillLevels: number[];
    defense: number;
    resistence: number[];
    grade: number[];
}
